using UnityEngine;
using System.Collections;

public class PlayerSaveSpot : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Color c = new Color(0.5f,0.5f,0.5f,0.5f);
		renderer.material.color = c;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
